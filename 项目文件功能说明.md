# 项目文件功能说明

> 本文件记录 bps2api 派生过程中**新增/修改/删除**的文件及其职责。
> 基线：https://github.com/lvzhentao/chatgpt-prism2api（module 由 `prism-2api` 改为 `bps-2api`）。

## 新增：上游适配器 `internal/adapter/bps/`

| 文件 | 功能 |
|---|---|
| `site.go` | `adapter.Site` 实现：New / Name / NewClient / NewCatalog / Auth / MapChat；默认上游 `https://bps.openai.com/basispoints/api` |
| `endpoints.go` | 端点常量：`/responses`、`/responses/compact`、OAuth token 端点、client_id、scope |
| `credentials.go` | 凭据解析与编码：ChatGPT auth JSON / 裸 JWT / `rt.` / cookie 片段 / bundle；JWT 提取 `chatgpt_account_id` / email / plan；`BuildImportBundle`、`EncodeSecret`、`newID` |
| `refresh.go` | ChatGPT OAuth 刷新（`POST https://auth.openai.com/oauth/token`，form 表单），失败分类与重试 |
| `auth.go` | `LoginURL`（空）/ `PollLogin`（未实现）/ `ExchangeCredential`（refresh 优先，其次校验 access token） |
| `models.go` | 静态模型目录、默认模型 `gpt-5.6-sol`、模型别名解析、`reasoning.effort` 映射（max→xhigh） |
| `map.go` | 内核 `ChatRequest` → bps 请求体：system 合并、图片 data URL、`function_call(_output)` 历史、工具定义、生成 task/turn |
| `stream.go` | Responses SSE → `adapter.Event`：文本/思考/工具调用/用量/失败 |
| `client.go` | 出站客户端：认证头、UA/身份头、POST `/responses`、`upstreamError`（`StatusCode()`）、`FetchUsage`（JWT 身份） |
| `mock.go` | `-mock` 模式的假上游客户端 |
| `credentials_test.go` | 凭据解析/编码单测 |
| `map_test.go` | 请求体组装/模型解析单测 |
| `stream_test.go` | SSE 映射单测（含失败帧、客户端断开、坏帧） |
| `client_test.go` | httptest 假上游：请求头/体断言、401 分类、OAuth 刷新与 ExchangeCredential |

## 新增：其它

| 文件 | 功能 |
|---|---|
| `internal/persist/file.go` | 无 PostgreSQL 时的 JSON 文件存储后端（kind/id.json，原子写） |
| `docs/bps适配/ALIGNMENT_bps适配.md` | 需求对齐（范围/决策/验收） |
| `docs/bps适配/DESIGN_bps适配.md` | 架构设计（模块/接口/数据流/异常） |
| `docs/bps适配/TASK_bps适配.md` | 原子任务拆解与依赖图 |
| `docs/bps适配/ACCEPTANCE_bps适配.md` | 验收记录（含真机端到端证据） |
| `docs/bps适配/FINAL_bps适配.md` | 交付总结 |
| `docs/bps适配/TODO_bps适配.md` | 待办与缺失配置 |
| `docs/bps适配/部署说明_excel.niubiai.cc.md` | 生产部署记录（服务器路径/nginx/证书/常用命令） |
| `项目文件功能说明.md` | 本文件 |

## 修改：内核接线与业务

| 文件 | 改动 |
|---|---|
| `cmd/server/main.go` | 站点适配器由 prism 换为 bps |
| `internal/api/account_import.go` | CSV 导入改为 bps 凭据列（email/access_token/refresh_token/account_id/proxy），重写为本地解析 |
| `internal/api/machine_import.go` | 机器导入改收 token 推送（access/refresh/account_id/proxy），去掉登录任务 |
| `internal/api/admin_tasks.go` | 删除 sidecar 登录动作与辅助函数；`login` 动作返回“不支持，请导入凭据” |
| `internal/api/pool_api.go` | 账号池页改为展示邮箱/账号 ID/套餐/可续期/到期，按钮去掉登录 |
| `internal/api/admin.go` / `server.go` | 无 token 新增账号不再发起浏览器登录，直接报错 |
| `internal/pool/pool.go` | `StartBrowserLogin` 返回“不支持浏览器登录” |
| `internal/admin/config.go` | 注入提示词默认 `off`；env 改为 `BPS_SYSTEM_PROMPT(_FILE)`；env 有文本时自动 `inject` |
| `internal/api/prompt.go` / `prompt_test.go` | 文案注释/日志前缀改 bps；测试同步 |
| `internal/api/server.go` | 存储改为“PG → 文件 → 内存”三级；`prism:` 日志前缀清理；Extra 键改 `bps_` |
| `internal/config/config.go` | 默认上游/站点/模型/客户端版本改为 bps 值；默认不自动开浏览器 |
| `internal/accountfile/parse.go` | 纯文本行识别支持 `rt.` / `access_token=` / `refresh_token=` |
| `internal/adminapi/import_parse.go` | `----` 行与单行支持 refresh token 提取 |
| `internal/api/prewarm_boot_test.go` | MRU 测试加时间间隔（Windows 时钟粒度下稳定） |
| `internal/api/server_store_test.go` | 改为断言无库时文件存储回退 |
| `internal/api/responses_test.go` / `empty_completion_test.go` | 绑定 bps mock；错误文案前缀改 bps |
| `internal/api/admin_tasks_test*`（如存在） | 同步去除登录断言 |

## 修改：品牌/部署

| 文件 | 改动 |
|---|---|
| `README.md` | 重写为 bps-2api 使用说明 |
| `.env.example` | 变量改 `BPS_*`，上游指向 bps，说明文件存储模式 |
| `docker-compose.yml` | 删除两个登录侧车服务；服务名/变量改 bps；DATABASE_URL 可注释退化为文件存储 |
| `Dockerfile` | 只构建网关二进制 `bps2api`，去掉登录 CLI |
| `entrypoint.sh` | 启动二进制改名 |
| `.github/workflows/build.yml` | 只构建 app 镜像 |
| `scripts/deploy.sh` / `scripts/deploy-pull.sh` | 变量改名、去掉 login 镜像与 sidecar 引用 |
| `web/src/lib/brand.ts` / `web/index.html` / `web/package.json` | 品牌名/域名/标题/包名改 bps |

## 删除

| 路径 | 原因 |
|---|---|
| `internal/adapter/prism/` | 被 `internal/adapter/bps/` 取代 |
| `cmd/login/`、`sidecar/`、`Dockerfile.login` | 不做浏览器登录 sidecar |
| `scripts/login-loop.sh`、`scripts/renew_loop.py`、`scripts/restore_pool.py` | 登录/侧车专用脚本 |
